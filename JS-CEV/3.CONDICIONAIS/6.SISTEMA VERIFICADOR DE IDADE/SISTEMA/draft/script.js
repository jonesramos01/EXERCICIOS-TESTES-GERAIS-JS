function verificar(){
    //DATA ATUAL (AUTOMÁTICO):
    var dataAtual = new Date();
    var anoAtual = dataAtual.getFullYear();

    //DATA DO FORMULÁRIO:
    var anoForm = window.document.getElementById("txtano");

    //RESULTADO:
    var res = window.document.getElementById("res");
    
    //IMAGEM:


    if(idade == 0 || idade == ""){
        //alert(`[ERRO] Por favor, entre com os dados corretamente!`);

    } else if(idade ){

    }
}